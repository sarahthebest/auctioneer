import Button from '@mui/material/Button';

export default function BidBtn() {
  return (
      <Button variant="contained" color="success" sx={{width:'fit-content', height:'fit-content'}}>Bid</Button>
  );
}